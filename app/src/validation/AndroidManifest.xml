<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application android:label="VozLocal Validation" tools:replace="android:label">
        <!-- Native tests do not require cross-app access. Keep the banking setup untouched. -->
        <service android:name="dev.sebastian.vozlocal.service.DictationAccessibilityService"
            tools:node="remove" />
    </application>
</manifest>
